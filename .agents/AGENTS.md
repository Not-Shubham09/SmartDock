# Custom Rules for SmartDock Workspace

- When suggesting or executing Git commits, always use descriptive plain-language summaries (e.g. "added SettingsManager", "modified setup in main.cpp", "changed theme properties", "fixed typo") instead of structured styles like Conventional Commits (e.g. "feat:", "fix:").
